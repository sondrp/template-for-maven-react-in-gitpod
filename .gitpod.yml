image:
  file: .gitpod.Dockerfile


tasks:
  - init: sdk use java 16.0.1.hs-adpt &&
          cd gitpodtesting/mavenprojectest/backend &&
          mvn spring-boot:start
    command: cd ..

# List the ports to expose. Learn more https://www.gitpod.io/docs/config-ports/
ports:
  - port: 6080
    onOpen: notify
